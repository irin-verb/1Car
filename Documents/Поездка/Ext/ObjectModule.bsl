
Процедура ОбработкаПроведения(Отказ, Режим)
    Если ЭтотОбъект.Завершена Тогда
		Движения.БалансВодителей.Записывать = Истина;
		Движение = Движения.БалансВодителей.Добавить();
		Движение.ВидДвижения = ВидДвиженияНакопления.Приход;
		Движение.Период = Дата;
		Движение.Водитель = Водитель;
		Движение.Деньги = СтоимостьПоездки * ЭтотОбъект.Пассажиры.Количество();
	КонецЕсли;
КонецПроцедуры
